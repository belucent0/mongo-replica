.env
.env.local

# init-view.js: 배포처마다 다른 뷰 정의(스키마 노출 위험)이므로 패키지에 포함하지 않는다.
# 필요한 배포(예: 사내)에선 별도 채널로 scripts/init-view.js 를 제공한다.
# entrypoint 는 파일이 없으면 뷰 생성 단계를 자동으로 건너뛴다.
scripts/init-view.js
.commit-msg.tmp
