# Linux 컨테이너에서 실행되는 모든 스크립트는 반드시 LF 만 사용
# (Windows 의 git autocrlf 가 CRLF 로 변환하면 shebang 깨져서 exec 실패)
*.sh    text eol=lf
*.js    text eol=lf
Dockerfile text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf
.env*   text eol=lf
